% Appendix D, the tetrahedral card, band 2 --- Decker Fig. 7 as the card draws it.
%
% circuit_dec_tet.tex with exactly three differences, and the generator
% (code/_build_povm_cards.py, assertion 14) grid-diffs this file against that
% one cell by cell and admits no others:
%   (i)   one inserted column on the data row carrying the optional pre-rotation
%         \gate{U_g}, fenced by its own dashed \gategroup{2}{2}{2}{2}{.7em}{--},
%         and one \qw in that column on every other wire;
%   (ii)  the reorientation drawn as the named box \gate{U_R = T^\dagger} --- the
%         chapter head's "written $U_R$" made literally true WITHOUT unsaying
%         Appendix D.2's "the tetrahedron's and the cube's correction is
%         drawn, the $T^\dagger$ of Figures D.1 and D.3", which stays true
%         verbatim.  This is the exactness split made visible in the
%         drawing: two of the five reorientations are Clifford+T gates and are
%         drawn as gates; three are angles and are drawn as a name.
%   (iii) the solid \gategroup's column indices shifted by exactly +1.
% Both new gates sit OUTSIDE the solid \gategroup, which still fences Decker's
% published block exactly as before: reorient Decker to the atlas, never the
% reverse -- what the fence encloses must stay Decker's circuit as published.
% The pose is unchanged and the generator proves it numerically, not by care.
%
% Set flush left on the card, never centred: all of xy-pic's ~13.7bp of slack is
% on the right, and _circuit_preamble's border={14pt 0.5pt 14pt 0.5pt} is what
% keeps the \lstick |0> and rho labels from being clipped.  Include at natural
% size --- no width=.
\input{_circuit_preamble}
\begin{document}
$$
\Qcircuit @C=2.0em @R=1.8em {
    \lstick{\ket{0}} & \qw        & \qw                    & \targ     & \gate{U_A} & \ctrl{1} & \qw      & \meter \gategroup{1}{4}{2}{8}{1.1em}{-} \\
    \lstick{\rho}    & \gate{U_g} \gategroup{2}{2}{2}{2}{.7em}{--} & \gate{U_R = T^\dagger} & \ctrl{-1} & \qw        & \gate{S} & \gate{H} & \meter
}
$$
\end{document}
